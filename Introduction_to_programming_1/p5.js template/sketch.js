function setup()
{
	createCanvas(800, 600);
	background(0, 0, 0);
}

function mousePressed() {
	fill(0, 0, 250);
	ellipse(mouseX,mouseY, 10, 10);

	
}

if (mouseIsPressed === true) {
	fill(0, 0, 250);
	ellipse(mouseX,mouseY, 10, 10);
  }
